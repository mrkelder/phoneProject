<!DOCTYPE html>
html(lang=`${ header.lang === 'ua' ? 'ua' : 'ru'}`)
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/css/index.css")
        title PhoneShop
    body
        include include/header.pug
        include include/slider.pug
        include include/item.pug
        main
          +slider(slides , 'slider' , 'slider_block' , 'slider_panel')
          #quality
            .qualityBlock
              .qualityBlockImg
              p #{header.lang === 'ua' ? 'Поповнення рахунку без комісії' :'Пополнение счета без комиссии'}
            .qualityBlock
              .qualityBlockImg
              p #{header.lang === 'ua' ? 'Безкоштовна доставка в магазини' :'Бесплатная доставка в магазины'}
            .qualityBlock
              .qualityBlockImg
              p #{header.lang === 'ua' ? 'Офіційна гарантія від виробника' :'Официальная гарантия от производителя'}
          p#best_for_today #{header.lang === 'ua' ? 'Найкращі пропозиції на сьогодні' :'Лучшие предложения на сегодня'}
          +slider(top_items , 'topItemsSlider' ,'topItemsSliding' , 'topItemsPanel')
        include include/footer.pug
        script(src="/js/slider.js")
        script(src="/js/item.js")
        script(src="/js/index.js")